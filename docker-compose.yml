version: "3"
services:
  resumeapi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:10000"   # localhost:3001 → container:10000
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
